<p id="notice"><%= notice %></p>
<div class="box2">
    <h3>Gêneros<h3><p>
</div>

<div class = "box1">
  <table class="table table-bordered table-dark">
    <thead>
      <tr>
        <th><h3 style="font-size:200%; font-family:verdana; color:#f2f2f2;">Nome</h3></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @generos.each do |genero| %>
        <tr>
          <td><%= genero.nome %></td>
          <td><%= link_to 'Ver Filmes', genero, class: "btn btn-default" %></td>
          <td><%= link_to 'Editar', edit_genero_path(genero), class: "btn btn-default" %></td>
          <td><%= link_to 'Deletar', genero, method: :delete, data: { confirm: 'Tem certeza que deseja deletar?' }, class: "btn btn-danger" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  </div>
<br>

<%= link_to 'Adicionar novo Genero', new_genero_path, class: "btn btn-default", style:"background-color: #d9d9d9" %>
